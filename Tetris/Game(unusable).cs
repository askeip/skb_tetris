using System;
using System.Collections.Generic;
using System.Collections.Immutable;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Tetris
{
    /*class Game
    {
        public void StartGame()
        {
            GameField gameField = new GameField(4, 6);
        }
    }

    partial class GameField
    {
        ImmutableArray<ImmutableArray<Point>> gameField;
        public GameField(int width,int height)
        {
            ImmutableArray<ImmutableArray<Point>>[] mutableGameField = new ImmutableArray<ImmutableArray<Point>>[height];
            //ImmutableArray<Point> emptyLine
            for (int i = 0; i < height; i++)
            {
                mutableGameField[i] = ImmutableArray.Create<ImmutableArray<Point>>();
            }
                gameField = ImmutableArray.Create<ImmutableArray<Point>>(
                    ImmutableArray.Create<Point>(new Point(width, height)));
        }
    }*/
}
